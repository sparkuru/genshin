services:
  httpd:
    image: httpd:alpine
    container_name: httpd
    volumes:
    - ./htdocs:/usr/local/apache2/htdocs:ro
    ports:
    - 8083:80
    restart: always

# visit: http://127.0.0.1:8083
# docker-compose -f $PWD/httpd.yml up -d
