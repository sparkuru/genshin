model = "gpt-6-astra"
model_reasoning_effort = "high"
service_tier = "default"
personality = "pragmatic"
approvals_reviewer = "auto_review"

developer_instructions = """
High-capability orchestration mode is active for this profile.

Do not select the `sub_agent` agent. Delegate independently useful work to the
built-in `default`, `worker`, or `explorer` agents when it helps; let them
inherit this profile's model and reasoning effort unless explicitly overridden.
Wait for delegated results and verify their evidence before relying on them.
"""

[tui.model_availability_nux]
gpt-6-astra = 1

[features]
hooks = true
fast_mode = false
multi_agent = true

[hooks.state]

[hooks.state."/home/wkyuu/.codex/hooks.json:session_start:0:0"]
trusted_hash = "sha256:68ae78cc417f211f7e770ee854b625bd8178f8ed6285787c421484ce5da2ec95"

# projects' trusted config

[projects."/tmp/tmp"]
trust_level = "trusted"
