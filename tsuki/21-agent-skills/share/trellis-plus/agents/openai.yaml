interface:
  display_name: "Trellis Plus"
  short_description: "Add shared Trellis Plus rules without patching Trellis runtime"
  default_prompt: "Use $trellis-plus to inspect this Trellis project, record safe project-shared rules and narrow local settings, and leave Trellis runtime files unchanged."
